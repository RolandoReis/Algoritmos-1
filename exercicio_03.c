// int main(): Função principal do programa, onde a execução começa.
// int a, b, c;: Declara três variáveis inteiras chamadas a, b e c.
// printf("Insira um valor para a, b e c:\n");: Mostra uma mensagem pedindo ao usuário para inserir três números.
// scanf("%d %d %d", &a, &b, &c);: Lê três valores inteiros do teclado e os armazena nas variáveis a, b e c.



#include <stdio.h>

int main(){
    int a, b, c;

    printf("insira um valor para a, b e c:\n");

    scanf("%d %d %d", &a, &b, &c);

    return 0;
}
